#!/usr/bin/env ruby

# A simple command line interface for the Transposon Agent and
# message routing mesh

require 'English'
require 'transposon'
require 'transposon/client/examples/command_line_interface'

command_line_arguments = ARGV

interface = Transposon::Client::Examples::CommandLineInterface.new
interface.run!(command_line_arguments)
